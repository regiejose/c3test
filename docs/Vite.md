# Vite + Tailwind Setup

Vite was selected because it supports both Tailwind and React build workflows with fast HMR and simplified asset bundling.


## Install

```bash
npm install tailwindcss @tailwindcss/vite vite
```
